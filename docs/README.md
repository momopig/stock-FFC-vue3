# 帮助中心文档系统

基于 VitePress 构建的帮助中心文档系统，提供功能使用说明和操作指南。

## 🚀 快速开始

### 开发环境

1. **启动主应用**
   ```bash
   npm run dev
   ```
   访问：<!-- http://localhost:5173 -->

2. **启动文档站**
   ```bash
   npm run docs:dev
   ```
   访问：<!-- http://localhost:5174 -->

### 生产环境

1. **构建所有内容**
   ```bash
   npm run build:all
   ```

2. **部署到服务器**
   - 主应用：`dist/` 目录
   - 帮助中心：`dist/help/` 目录

## 📁 目录结构

```
docs/                          # VitePress 文档站
├── .vitepress/
│   └── config.js              # VitePress 配置
├── guide/                     # 使用指南
├── features/                  # 功能模块文档
│   ├── activity/              # 活动管理
│   ├── order/                 # 订单管理
│   └── product/               # 商品管理
├── public/
│   └── images/                # 图片资源
└── index.md                   # 文档首页

scripts/
├── copy-docs.js               # 复制脚本
└── build-help.sh              # 构建脚本
```

## 🛠️ 开发指南

### 添加新文档

1. 在 `docs/features/` 下创建新的功能目录
2. 添加 `.md` 文件
3. 在 `docs/.vitepress/config.js` 中配置侧边栏
4. 添加相关图片到 `docs/public/images/`

### 文档编写规范

- 使用 Markdown 语法
- 图片放在 `docs/public/images/` 目录
- 使用 VitePress 的容器语法（tip、warning、danger）
- 添加适当的标题层级
- **图片预览**：所有图片自动支持点击放大查看

### 图片预览功能

使用vitepress-plugin-image-viewer插件实现图片预览功能。


### 示例文档

```markdown
# 功能名称

## 功能概述
简要描述功能的作用和用途。

## 操作步骤
1. 第一步
2. 第二步
3. 第三步

::: tip 提示
这是一个提示信息
:::

::: warning 注意事项
这是注意事项
:::
```

## 🔧 配置说明

### VitePress 配置

主要配置项：
- `title`: 文档标题
- `base`: 部署路径（/help/）
- `themeConfig.nav`: 导航栏
- `themeConfig.sidebar`: 侧边栏
- `search.provider`: 搜索配置

### 构建脚本

- `docs:dev`: 开发模式启动文档站
- `docs:build`: 构建文档站并复制到 dist/help
- `docs:preview`: 预览构建结果
- `build:all`: 构建主应用和文档站

## 🌐 部署配置

### Nginx 配置

```nginx
# 主应用
location / {
  root /var/www/shop-frontend-vue3/dist;
  try_files $uri $uri/ /index.html;
}

# 帮助中心
location /help/ {
  alias /var/www/shop-frontend-vue3/dist/help/;
  try_files $uri $uri/ /help/index.html;
}
```

## 📝 使用说明

### 用户操作

1. 点击顶部导航栏的"帮助中心"按钮
2. 在新标签页中打开帮助文档
3. 使用左侧目录树导航
4. 使用顶部搜索框查找内容

### 管理员操作

1. 编辑 `docs/` 目录下的 Markdown 文件
2. 运行 `npm run docs:build` 重新构建
3. 部署更新后的文件

## 🎯 功能特性

- ✅ 自动目录树生成
- ✅ 本地搜索功能
- ✅ 响应式布局
- ✅ 深色模式支持
- ✅ 代码高亮
- ✅ 图片懒加载
- ✅ **图片预览功能**（点击放大、键盘导航、拖拽缩放）
- ✅ 自定义容器
- ✅ 最后更新时间

## 🔍 故障排除

### 常见问题

1. **VitePress 构建失败**
   - 检查 `docs/.vitepress/config.js` 语法
   - 确保所有引用的文件存在

2. **图片无法显示**
   - 检查图片路径是否正确
   - 确保图片文件存在于 `docs/public/images/`

3. **搜索功能不工作**
   - 确保配置了 `search.provider: 'local'`
   - 重新构建文档站

### 调试方法

1. 使用 `npm run docs:dev` 在开发模式下调试
2. 检查浏览器控制台错误信息
3. 查看 VitePress 构建日志

## 📞 技术支持

如有问题，请联系开发团队或查看 VitePress 官方文档：
- [VitePress 官方文档](https://vitepress.dev/)
- [Markdown 语法指南](https://vitepress.dev/guide/markdown)
