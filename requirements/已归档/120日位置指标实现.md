1、在所有股票列表，在'20日涨跌'字段后面，新增一个字段，‘120日位置指标’，展示quote.ma_response.price_location_indicator.value，
其中quote.ma_response.price_location_indicator格式为：
```
{
    daysRange: 120, // 最近120日
    value: 0.9, // 估计位置指标，当前价格除以120日的最高价格
    desc: {
        level: '高位(回调区间在[-10%, 0]，不便宜)',
        range: '[0.9, 1]',
    },
    rules: {
        '高位(回调区间在[-10%, 0]，不便宜)': '[0.9, 1]',
        '中等(回调区间在[-20, -10%),多观望)': '[0.8, 0.9)',
        '偏低(回调区间在[-30, -20%),看介入)': '[0.7, 0.8)',
        '超跌(回调区间在[-100%, -30%),看捡漏)': '(0, 0.7)'
    },
    maxHeightFallRate: -10, // 最高回落比例
    changeRate: 1.2 // 最近N日涨跌
}
```
2、在quote.ma_response.price_location_indicator.value下面，展示最近daysRange日的maxHeightFallRate和changeRate值，需要加%,负号显示绿色，正号显示红色，值为0显示灰色,格式为：
```
  0.9
  最高回落比例：-10%
  ${daysRange}日涨跌：1.2%
```
；

3、鼠标hover该值时，展示quote.ma_response.price_location_indicator.desc；
4、表格字段排序(sortable)按value排序；
